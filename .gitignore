# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# C++ build output
build/
*.o
*.a
*.so

# Model weights are not distributed with this repository — each carries its own
# licence and is fetched at deploy time. See "Models and their licences".
*.rknn
*.hef
*.engine
*.cvimodel
*.pt
*.onnx

# Capture and test media
testdata/
*.mp4
# MQTT captures, which are throwaway. The contract fixtures are not captures —
# they are the test inputs CI runs the validator against, so they stay tracked.
*.jsonl
!contracts/fixtures/*.jsonl

# macOS
.DS_Store
